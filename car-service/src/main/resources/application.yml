server:
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.grigoryev: DEBUG

spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: mongo1
      port: 27017
      database: car
      username: root
      password: password
      authentication-database: admin
